<Window x:Class="BirthdayApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BirthdayApp.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Виберіть дату народження:" Margin="0,0,0,10"/>
            <DatePicker SelectedDate="{Binding BirthDate}" Margin="0,0,0,20"/>

            <Button Content="Підтвердити" Command="{Binding ConfirmDateCommand}" Margin="0,0,0,20"/>

            <TextBlock Text="{Binding AgeText}" FontSize="16" Margin="0,0,0,10"/>

            <TextBlock Text="Західний знак зодіаку:" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding WesternZodiac}" FontSize="16" Margin="0,0,0,10"/>

            <TextBlock Text="Китайський знак зодіаку:" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding ChineseZodiac}" FontSize="16"/>
        </StackPanel>
    </Grid>
</Window>
