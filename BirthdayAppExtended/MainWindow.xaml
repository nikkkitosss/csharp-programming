<Window x:Class="BirthdayAppExtended.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BirthdayAppExtended.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid  Margin="20,0">
        <StackPanel>
            <TextBlock Text="Ім'я:" FontSize="16"/>
            <TextBox Text="{Binding FirstName}" Width="200" Height="30"/>

            <TextBlock Text="Прізвище:" FontSize="16"/>
            <TextBox Text="{Binding LastName}" Width="200" Height="30"/>

            <TextBlock Text="Email:" FontSize="16"/>
            <TextBox Text="{Binding Email}" Width="200" Height="30"/>

            <TextBlock Text="Дата народження:" FontSize="16"/>
            <DatePicker SelectedDate="{Binding BirthDate}" Width="200" Height="30"/>

            <Button Content="Proceed" Command="{Binding ProceedCommand}" Width="100" Height="30" Margin="0,10,0,0" />

            <TextBlock Text="{Binding OutputFullName}" FontSize="16" />

            <TextBlock Text="{Binding OutputBirthDate}" FontSize="16" />

            <TextBlock Text="{Binding OutputEmail}" FontSize="16"/>

            <TextBlock Text="{Binding AgeText}" FontSize="16" />
            
            <TextBlock Text="{Binding WesternSign}" FontSize="16" />

            <TextBlock Text="{Binding ChineseSign}" FontSize="16" />

            <TextBlock Text="{Binding OutputIsAdult}" FontSize="16" />
        </StackPanel>
    </Grid>

</Window>
